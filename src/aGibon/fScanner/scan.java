package aGibon.fScanner;
import java.util.Scanner;

public class scan {
    //패키지 아래에 를 써서 한다음에 메인문에
    public static void main(String[] args) {
        Scanner 이름 = new Scanner(System.in);
        int a = 이름.nextInt();
        //이런식으로 쓴다.
    }
}
